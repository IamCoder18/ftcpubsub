---
title: Get Started
description: Install Synapse and write your first OpMode in under five minutes.
---

# Get Started

This section gets you from `git clone` (or `gradle sync`) to a working `SafeOpMode`.

- **[Install](/docs/get-started/install)** — Add the dependency, apply keep rules, and pick a build system.
- **[First OpMode](/docs/get-started/first-opmode)** — A copy-paste `TeleOp` that drives a single motor from a gamepad axis.
